{
  "name": "bennuide-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "BennuIDE - Integrated Development Environment for BennuGD2",
  "workspaces": [
    "extensions/*",
    "packages/*"
  ],
  "scripts": {
    "compile": "npm run compile --workspaces --if-present",
    "build": "npm run compile --workspaces --if-present",
    "package:all": "npx -y @vscode/vsce package --no-dependencies -o dist/ --prefix extensions/bennugd2-language && npx -y @vscode/vsce package --no-dependencies -o dist/ --prefix extensions/bennugd2-fpg-editor && npx -y @vscode/vsce package --no-dependencies -o dist/ --prefix extensions/bennugd2-fnt-editor && npx -y @vscode/vsce package --no-dependencies -o dist/ --prefix extensions/bennugd2-audio-editor && npx -y @vscode/vsce package --no-dependencies -o dist/ --prefix packages/bennuide-ai-agent",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "keywords": [
    "bennugd",
    "bennugd2",
    "ide",
    "gamedev",
    "fpg",
    "fnx",
    "fnt"
  ],
  "author": "Juan Luis Ramírez Tutor",
  "license": "MIT",
  "devDependencies": {
    "7zip-bin": "^5.2.0",
    "7zip-bin-mac": "^1.0.1",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
